import { GetAssetUrlPipe } from './get-asset-url.pipe';

describe('GetAssetUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new GetAssetUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
