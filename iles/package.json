{
  "name": "directus-iles-example",
  "private": true,
  "scripts": {
    "dev": "iles dev --open",
    "build": "iles build",
    "preview": "iles preview --open --port 5050"
  },
  "engines": {
    "node": "^14.18 || >= 16.0.0"
  },
  "dependencies": {
    "@directus/sdk": "9.10.0"
  },
  "devDependencies": {
    "@islands/icons": "^0.1.1",
    "@types/node": "^17.0.23",
    "iles": "^0.7.37",
    "modern-normalize": "^1.1.0"
  }
}
